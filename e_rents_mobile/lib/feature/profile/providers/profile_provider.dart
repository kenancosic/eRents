import 'package:e_rents_mobile/core/base/base_provider.dart';

/// Consolidated provider for all Profile feature functionality
/// Manages user profile, bookings, tenant preferences, and payment methods
/// Refactored to use new standardized BaseProvider without caching
class ProfileProvider extends BaseProvider {
  
  ProfileProvider(super.api);

  // ─── State ──────────────────────────────────────────────────────────────
  // All state has been moved to more specific providers.

  // All methods have been moved to more specific providers.

}
